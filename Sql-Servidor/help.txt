Connection conn = SqlServidor.connection();
if(conn != null){
	String sql = "select * from";
	ResultSet rs = conn.prepareStatement(sql).executeQuery();
	while (rs.next()){
		System.out.println(
			rs.getInt() + " | " +
			rs.getString()
		);
	}
	conn.close();
}
